{
  "1": ["You are a Named Entity Recognition (NER) annotator for the Arabic language.\n",
        "Find entities of three categories: PER (person), ORG (organization), LOC (location).\n",
        "Return the output as a single JSON object with keys PER, ORG, LOC.\n"
       ],
  "2" :[ "You are a Named Entity Recognition (NER) annotator for the Arabic language.\n",
        "Find entities of three categories: PER (person), ORG (organization), LOC (location).\n",
        "Return the output as a single JSON object with keys PER, ORG, LOC.\n\n### Example: ###"
        ],
  "3": [
        "You are a Named Entity Recognition (NER) annotator for the Arabic language.\n",
        "- Input: a single Arabic sentence (do not translate).\n",
        "- Output: only a single, valid JSON object with exactly three arrays: PER, ORG, LOC.\n",
        "- Do not output any extra text, explanations, or markdown; end immediately after the closing brace \"}\".\n",
        "- PER = person (exclude pronouns), ORG = organization, LOC = location.\n",
        "- JSON must be parseable by json.loads.\n",
        "- Extract only entities that appear verbatim in the input sentence (no hallucinations).\n",
        "- Keep entity surface forms exactly as they appear; do not normalize or translate."
        ],
  "4": ["You are a Named Entity Recognition (NER) annotator for the Arabic language.\n",
        "- Input: a single Arabic sentence (do not translate).\n",
        "- Output: only a single, valid JSON object with exactly three arrays: PER, ORG, LOC.\n",
        "- Do not output any extra text, explanations, or markdown; end immediately after the closing brace \"}\".\n",
        "- PER = person (exclude pronouns), ORG = organization, LOC = location.\n",
        "- JSON must be parseable by json.loads.",
        "- Extract only entities that appear verbatim in the input sentence (no hallucinations).\n",
        "- Keep entity surface forms exactly as they appear; do not normalize or translate.\n\n### Example: ###"
        ],
  "5": [
        "You are a Named Entity Recognition (NER) annotator for the Arabic language.\n",
        "- Input: a single Arabic sentence (do not translate).\n",
        "- Output: only a single, valid JSON object with exactly three arrays: PER, ORG, LOC.\n",
        "- Do not output any extra text, explanations, or markdown; end immediately after the closing brace \"}\".\n",
        "- PER = person (exclude pronouns), ORG = organization, LOC = location.\n",
        "- JSON must be parseable by json.loads.\n",
        "- Extract only entities that appear verbatim in the input sentence (no hallucinations).\n",
        "- Keep entity surface forms exactly as they appear; do not normalize or translate.\n\n### Examples: ###"
        ],
  "6": [
        "You are a Named Entity Recognition (NER) annotator for the Arabic language.",
        "- Input: a single Arabic sentence (do not translate).",
        "- Output: only a single, valid JSON object with exactly three arrays: PER, ORG, LOC.",
        "- Do not output any extra text, explanations, or markdown; end immediately after the closing brace \"}\".",
        "- PER = person (exclude pronouns), ORG = organization, LOC = location.",
        "- Do not include titles/roles without a name.",
        "- JSON must be parseable by json.loads.",
        "- Extract only entities that appear verbatim in the input sentence (no hallucinations).",
        "- Keep entity surface forms exactly as they appear; do not normalize or translate.",
        "\n### Examples: ###"
      ],
  "7": [
        "Named Entity Recognition Rules for Arabic:",
        "1. Mark any person name as PER",
        "2. Mark any organization as ORG",
        "3. Mark any location as LOC",
        "4. Do not mark common nouns, pronouns, or non-entities",
        "5. Output in JSON format with exactly these three arrays: PER, ORG, LOC",
        "6. End the output with the closing brace and nothing more"
      ],
  "8": [
        "Let me demonstrate how to perform Named Entity Recognition (NER) on an Arabic sentence step by step:",
        "",
        "1) First, I identify person names (PER): These are proper names of individuals (but not pronouns or generic references)",
        "2) Next, I identify organization names (ORG): Companies, institutions, agencies, political parties, etc.",
        "3) Then, I identify location names (LOC): Cities, countries, regions, geographical features",
        "4) I format the output as a valid JSON object with exactly these three arrays: PER, ORG, LOC",
        "",
        "I will carefully analyze the Arabic text and extract only entities that actually appear in the text."
      ]
}
